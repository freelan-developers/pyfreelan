language: python
python:
  - "2.7"
install:
  - pip install -r dev_requirements.txt
  - pip install --editable .
script:
  - coverage run --source=pyfreelan setup.py nosetests
  - coverage report
after_success: coveralls
notifications:
    email:
        on_success: change
        on_failure: always
